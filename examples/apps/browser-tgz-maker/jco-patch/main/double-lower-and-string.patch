diff --git a/crates/js-component-bindgen/src/function_bindgen.rs b/crates/js-component-bindgen/src/function_bindgen.rs
index 18221df..e94cdad 100644
--- a/crates/js-component-bindgen/src/function_bindgen.rs
+++ b/crates/js-component-bindgen/src/function_bindgen.rs
@@ -2721,9 +2721,18 @@ impl Bindgen for FunctionBindgen<'_> {
                         ("-1".into(), "undefined".into())
                     };
 
-                uwriteln!(
-                    self.src,
-                    r#"
+                // Lower a future operand inline only for sync functions, or for
+                // async functions when the operand is an incoming parameter. For
+                // async host *imports* the operand is the function's return
+                // value, which is lowered by the async return-handling code
+                // (`task.resolve`); lowering it inline too would consume/settle
+                // the future and double-lower it.
+                if self.is_async && !self.params.iter().any(|p| p == future_arg) {
+                    results.push(future_arg.clone());
+                } else {
+                    uwriteln!(
+                        self.src,
+                        r#"
                         if (!{is_future_lowerable_object_fn}({future_arg})) {{
                             {debug_log_fn}('[Instruction::FutureLower] object is not a Promise/Thenable', {{ {future_arg} }});
                             throw new Error('unrecognized future object (not Promise/Thenable)');
@@ -2815,9 +2824,10 @@ impl Bindgen for FunctionBindgen<'_> {
 
                         const {lowered_future_waitable_idx} = readEnd{tmp}.waitableIdx();
                     "#
-                );
+                    );
 
-                results.push(lowered_future_waitable_idx);
+                    results.push(lowered_future_waitable_idx);
+                }
             }
 
             Instruction::FutureLift { payload, ty } => {
@@ -3066,9 +3076,20 @@ impl Bindgen for FunctionBindgen<'_> {
 
                 let tmp = self.tmp();
                 let lowered_stream_waitable_idx = format!("streamWaitableIdx{tmp}");
-                uwriteln!(
-                    self.src,
-                    r#"
+
+                // Lower a stream operand inline only for sync functions, or for
+                // async functions when the operand is an incoming parameter
+                // (e.g. an export lowering a `stream` param to hand to the
+                // guest). For async host *imports* the operand is the function's
+                // return value, which is lowered by the async return-handling
+                // code (`task.resolve`); lowering it inline too would
+                // consume/lock the stream and double-lower it.
+                if self.is_async && !self.params.iter().any(|p| p == stream_arg) {
+                    results.push(stream_arg.clone());
+                } else {
+                    uwriteln!(
+                        self.src,
+                        r#"
                         if (!({async_iterator_symbol} in {stream_arg})
                             && !({iterator_symbol} in {stream_arg})
                             && !({stream_arg} instanceof {external_readable_stream_class})) {{
@@ -3125,9 +3146,10 @@ impl Bindgen for FunctionBindgen<'_> {
 
                         const {lowered_stream_waitable_idx} = readEnd{tmp}.waitableIdx();
                     "#
-                );
+                    );
 
-                results.push(lowered_stream_waitable_idx);
+                    results.push(lowered_stream_waitable_idx);
+                }
             }
 
             Instruction::StreamLift { payload, ty } => {
diff --git a/crates/js-component-bindgen/src/intrinsics/mod.rs b/crates/js-component-bindgen/src/intrinsics/mod.rs
index 02d3d1a..47d6b54 100644
--- a/crates/js-component-bindgen/src/intrinsics/mod.rs
+++ b/crates/js-component-bindgen/src/intrinsics/mod.rs
@@ -1481,6 +1481,25 @@ pub fn render_intrinsics(args: RenderIntrinsicsArgs) -> Source {
     {
         args.intrinsics
             .insert(Intrinsic::String(StringIntrinsic::GlobalTextEncoderUtf8));
+        // The lowered-string body calls `_utf8AllocateAndEncode` (the
+        // `Utf8Encode` string intrinsic) to realloc guest memory and encode the
+        // string into it. Without this the helper is referenced but never
+        // emitted, throwing `ReferenceError: _utf8AllocateAndEncode is not
+        // defined` when a string is lowered as part of a stream/record payload.
+        args.intrinsics
+            .insert(Intrinsic::String(StringIntrinsic::Utf8Encode));
+    }
+
+    if args
+        .intrinsics
+        .contains(&Intrinsic::Lower(LowerIntrinsic::LowerFlatStringUtf16))
+    {
+        // Mirror of the UTF-8 case: the lowered-string body calls
+        // `_utf16AllocateAndEncode` (the `Utf16Encode` string intrinsic), which
+        // in turn needs `IsLE`. Emit both so the helper is defined.
+        args.intrinsics
+            .insert(Intrinsic::String(StringIntrinsic::Utf16Encode));
+        args.intrinsics.insert(Intrinsic::IsLE);
     }
 
     if args
