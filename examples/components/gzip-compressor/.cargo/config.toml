[build]
# Build a bare core module with no WASI imports, then `wasm-tools component new`
# (see the justfile) wraps it into a component. The result imports nothing -- it
# is a capability-free byte transform -- so it can be composed into any pipeline
# that needs the `compressor` interface.
target = "wasm32-unknown-unknown"
